# PixelChatRoom — 專案規格 & Claude Code 開發 Prompt

> 這份文件同時是規格書，也是可以直接貼給 Claude Code 的開發 prompt。

## 專案總覽

一個網頁版小型多人聊天遊戲，最多 5 人同時在線，畫面為復古像素 RPG 風格（原創角色設計，靈感來自 Gameboy 世代 RPG，非任何既有 IP 的角色圖案）。玩家進房前先自訂角色外觀，進房後可在單一房間內自由移動、打字聊天，聊天內容會以對話氣泡顯示在角色頭上，所有人可見。

## 技術棧

- **前端**：純 HTML5 Canvas + Vanilla JS，不使用額外的 game framework。像素以低解析度畫布（例如 320×240）搭配 `image-rendering: pixelated` 放大顯示，維持顆粒感。
- **後端**：Node.js + `ws` library，單一 process，狀態全部存在記憶體（不用資料庫）。
- **通訊**：WebSocket，訊息格式 JSON。
- **角色美術**：不用外部圖檔，改用程式化繪製——每個部位（髮型、服裝）定義成一個小型 2D 像素矩陣，用 canvas 逐格繪製，顏色依玩家選擇的色票即時上色。這樣髮型/服裝/顏色可以自由組合，也完全不需要美術素材檔案。

## 遊戲設計規格

### 移動
- 自由連續移動（WASD 或方向鍵，8 方向）
- **客戶端權威**：每個玩家自己的移動由本地端即時運算與碰撞判定（貼牆/貼家具不會穿模），移動結果直接送給 server
- Server 對收到的位置只做基本邊界檢查（不超出房間範圍），不重算家具碰撞，然後轉發給其他玩家 — 這是刻意簡化，因為只有 5 個熟人一起玩，不需要防作弊等級的伺服器權威驗證
- 位置更新頻率上限：約每 50ms 一次（避免洗版），非每個 frame 都送

### 角色外觀（模組化）
- 髮型、服裝、顏色三個維度分開選擇，各自有數款可選
- 進房前的角色選擇畫面需要即時預覽（换任一選項，預覽圖立刻更新）
- 同時輸入顯示名稱

### 房間
- 單一固定房間，內有數個家具/障礙物（矩形範圍），移動時不能穿過
- 房間資料（尺寸、家具位置）前後端要用同一份設定，因為前端要算碰撞、後端要算邊界
- 人數上限 5 人，第 6 個連線嘗試會被拒絕並顯示原因
- **房間實際尺寸與家具數量/位置的具體數值，本規格不預先指定**——請在 Module C 的 Plan Mode 階段提出具體提案（含座標），我確認過再實作，不要自己決定完就直接寫死

### 名稱輸入（邊界情況，已定案不用問）
- 名稱留白：用預設值（例如「玩家」+ 隨機碼）帶入，不擋住玩家進房
- 重複名稱：允許，不做唯一性檢查

### 聊天
- 底部輸入框送出文字
- 訊息會廣播給所有人，並在對應角色頭上顯示對話氣泡
- 氣泡顯示約 4-5 秒後自動淡出；新訊息進來直接取代舊氣泡

## WebSocket 訊息協定（草案，Claude Code 可依實作微調但需維持語意一致）

**Client → Server**
```
{"type":"join","name":"...","appearance":{"hair":n,"outfit":n,"color":n}}
{"type":"move","x":123.4,"y":56.7}
{"type":"chat","text":"..."}
```

**Server → Client**
```
{"type":"room_state","self_id":"...","players":[{"id":"...","name":"...","appearance":{...},"x":..,"y":..}],"furniture":[{"x":..,"y":..,"w":..,"h":..}]}
{"type":"player_joined","id":"...","name":"...","appearance":{...},"x":..,"y":..}
{"type":"player_moved","id":"...","x":..,"y":..}
{"type":"player_chat","id":"...","text":"..."}
{"type":"player_left","id":"..."}
{"type":"room_full","message":"房間已滿（最多 5 人）"}
```

`room_state` 裡的 `self_id` 是給剛連進來的 client 用來分辨「players 列表裡哪一筆是自己」——自己的角色由本地端即時運算移動（client 權威），其他人的角色才是靠後續的 `player_moved` 廣播更新，這兩種處理方式不一樣，client 必須先知道自己的 id 才能正確分流。

## 開發階段與驗收標準（給 Claude Code 依序執行）

**Module A — 專案骨架 + WebSocket 基礎連線**
驗收：本機啟動 server，兩個瀏覽器分頁都能連上 WebSocket，console 可看到 join/leave 訊息

**Module B — 角色選擇畫面**
驗收：切換髮型/服裝/顏色時預覽圖即時更新；送出後帶著完整 appearance 資料與名稱進入房間

**Module C — 房間渲染 + 移動 + 碰撞**
驗收：WASD 可自由移動角色；角色無法穿過家具與牆壁；畫面維持像素風格不因縮放模糊

**Module D — 多人位置同步**
驗收：開兩個以上分頁各自移動，其他分頁能即時看到對方位置更新

**Module E — 聊天氣泡**
驗收：任一分頁送出訊息，所有分頁都能在該角色頭上看到氣泡，且幾秒後自動消失

**Module F — 房間人數上限**
驗收：開第 6 個分頁嘗試連線會被拒絕並顯示原因

## 給 Claude Code 的操作指示（請務必遵守）

1. **先進 Plan Mode**：讀完這份規格後，先產出一份執行計畫（不要直接動手寫 code），跟我確認過計畫沒問題再開始實作。
2. **逐 Module 開發，不要一次全做完**：完成一個 Module 後，依照上面該 Module 的驗收標準自我驗證（實際跑起來檢查，不是只用你的判斷認為完成），驗證通過再進下一個 Module。
3. **每個 Module 完成並驗證通過後，做一次 git commit** 作為檢查點，commit message 要清楚寫這個 Module 做了什麼。
4. 目前是在本機 `/sandbox` 模式下運行，檔案寫入僅限本專案目錄內。
5. 若對規格中任何一點不確定或發現前後矛盾，先停下來問我，不要自行假設後繼續做。
